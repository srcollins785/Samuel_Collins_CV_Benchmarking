# Pinned to the versions the reported results were produced with.
#
# The package itself declares looser ranges in pyproject.toml, because a
# library should not force an exact dependency set on its users. This file is
# for reproducing the benchmark, where the point is to get the same numbers
# rather than the newest packages, so it names exact versions.
#
# Install for development and to reproduce everything:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#   pip install -e .
#
# YOLO classification needs a separate environment; see requirements-yolo.txt
# for why, and how.

# Part 1: the traditional ML and baseline neural benchmark.
numpy==2.0.2
pandas==2.3.3
pillow==11.3.0
scikit-learn==1.6.1
matplotlib==3.9.4

# Part 2: the deep CNN benchmark.
torch==2.8.0
torchvision==0.23.0

# Section 22, computational complexity. fvcore is tried first because it
# reports which operators it could not account for; thop is the fallback.
fvcore==0.1.5.post20221221
thop==0.1.1.post2209072238

# Test suite and packaging.
pytest==8.4.2
build==1.4.4
twine==6.2.0

# Report generation (scripts/, not the installed package).
markdown==3.9
